[package]
name = "vigy"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "vigy facade — re-exports every vigy-* crate so consumers (mado, tear-daemon, etc.) `cargo add vigy` once."

[features]
default = ["rest", "graphql", "rpc", "mcp"]
rest = ["dep:vigy-rest"]
graphql = ["dep:vigy-graphql"]
rpc = ["dep:vigy-rpc"]
mcp = ["dep:vigy-mcp"]

[dependencies]
vigy-types = { version = "0.1.1", path = "../vigy-types" }
vigy-store = { version = "0.1.1", path = "../vigy-store" }
vigy-eval = { version = "0.1.1", path = "../vigy-eval" }
vigy-runtime = { version = "0.1.1", path = "../vigy-runtime" }
vigy-rest = { version = "0.1.1", path = "../vigy-rest", optional = true }
vigy-graphql = { version = "0.1.1", path = "../vigy-graphql", optional = true }
vigy-rpc = { version = "0.1.1", path = "../vigy-rpc", optional = true }
vigy-mcp = { version = "0.1.1", path = "../vigy-mcp", optional = true }
